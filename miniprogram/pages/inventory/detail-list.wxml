<!--pages/inventory/detail-list.wxml-->
<view class="container p-3">
  <view wx:if="{{ loading && !hasLoadedOnce }}" class="page-loading-state">
    <van-loading type="spinner" vertical>加载中...</van-loading>
  </view>
  <block wx:elif="{{ list.length > 0 }}">
    <!-- 使用统一批次卡片组件 -->
    <batch-list-item
      wx:for="{{ list }}"
      wx:key="_id"
      item="{{ item }}"
      bind:tap="goToDetail"
      data-id="{{ item._id }}"
    />
  </block>
  <van-empty wx:elif="{{ hasLoadedOnce }}" description="暂无明细数据" />

  <view wx:if="{{ loading && hasLoadedOnce }}" class="inline-refresh-state">
    <van-loading size="20px">刷新中...</van-loading>
  </view>
  <view wx:if="{{ isEnd && list.length > 0 }}" class="inline-refresh-state">
    - 已无更多 -
  </view>
</view>
